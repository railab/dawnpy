${PROJECT_NAME}
================

This project was scaffolded by ``dawnpy project new``.

Build from this directory::

  python -m dawnpy build build boards/sim/sim/sim/configs/nsh_user_shell

The generated ``.dawnrc`` points back at the upstream Dawn checkout, so
``dawnpy`` and raw CMake can resolve the shared sources.
